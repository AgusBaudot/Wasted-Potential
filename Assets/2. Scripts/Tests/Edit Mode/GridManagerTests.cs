using NUnit.Framework;
using UnityEngine;

[TestFixture]
public class GridManagerTests
{
    private IGridQuery _gridManager;

    [SetUp]
    public void SetUp()
    {
        //I might need to create a minimal grid for testing
        //Or mock the grid if testing becomes complex
    }
    
    //world to grid converts correctly.
    
    // get tile returns null for invalid.
    
    //focus on testing what is easily testable.
}